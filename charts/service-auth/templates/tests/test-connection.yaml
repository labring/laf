apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "service-auth.fullname" . }}-test-connection"
  labels:
    {{- include "service-auth.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "service-auth.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
