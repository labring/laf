# This kustomization.yaml is not intended to be run by itself,
# since it depends on service name and namespace that are out of this kustomize package.
# It should be run by config/default
resources:
- bases/application.laf.dev_applications.yaml
- bases/application.laf.dev_specifications.yaml
- bases/application.laf.dev_runtimes.yaml
- bases/application.laf.dev_runtimepackages.yaml
- bases/application.laf.dev_cloudfunctions.yaml
- bases/application.laf.dev_triggers.yaml
#+kubebuilder:scaffold:crdkustomizeresource

patchesStrategicMerge:
# [WEBHOOK] To enable webhook, uncomment all the sections with [WEBHOOK] prefix.
# patches here are for enabling the conversion webhook for each CRD
#- patches/webhook_in_applications.yaml
#- patches/webhook_in_specifications.yaml
#- patches/webhook_in_runtimes.yaml
#- patches/webhook_in_runtimepackages.yaml
#- patches/webhook_in_cloudfunctions.yaml
#- patches/webhook_in_triggers.yaml
#+kubebuilder:scaffold:crdkustomizewebhookpatch

# [CERTMANAGER] To enable cert-manager, uncomment all the sections with [CERTMANAGER] prefix.
# patches here are for enabling the CA injection for each CRD
#- patches/cainjection_in_applications.yaml
#- patches/cainjection_in_specifications.yaml
#- patches/cainjection_in_runtimes.yaml
#- patches/cainjection_in_runtimepackages.yaml
#- patches/cainjection_in_cloudfunctions.yaml
#- patches/cainjection_in_triggers.yaml
#+kubebuilder:scaffold:crdkustomizecainjectionpatch

# the following config is for teaching kustomize how to do kustomization for CRDs.
configurations:
- kustomizeconfig.yaml
