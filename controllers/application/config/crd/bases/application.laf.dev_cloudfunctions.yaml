---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  name: cloudfunctions.application.laf.dev
spec:
  group: application.laf.dev
  names:
    kind: CloudFunction
    listKind: CloudFunctionList
    plural: cloudfunctions
    singular: cloudfunction
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: CloudFunction is the Schema for the cloudfunctions API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: CloudFunctionSpec defines the desired state of CloudFunction
            properties:
              allowedHTTPMethods:
                description: 'Allowed HTTP methods. If empty, http access is disabled.
                  The value of this field can be any one or a combination of the following:
                  GET, POST, PUT, DELETE, PATCH, HEAD, OPTIONS, CONNECT, TRACE. The
                  default values are: ["GET", "POST", "PUT", "DELETE"].'
                items:
                  type: string
                type: array
              code:
                description: Code for the function
                type: string
              compiledCode:
                description: Compiled code for the function
                type: string
              description:
                description: Description of function
                type: string
              name:
                description: Name of function
                type: string
              version:
                description: Version of the function
                format: int64
                type: integer
              webSocketEnabled:
                default: false
                description: WebSocket enabled, defaults to false
                type: boolean
            required:
            - allowedHTTPMethods
            - code
            - compiledCode
            - description
            - name
            - version
            - webSocketEnabled
            type: object
          status:
            description: CloudFunctionStatus defines the observed state of CloudFunction
            properties:
              state:
                default: Deployed
                description: State of the function
                enum:
                - Deployed
                type: string
            required:
            - state
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
