---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.0
  creationTimestamp: null
  name: users.oss.laf.dev
spec:
  group: oss.laf.dev
  names:
    kind: User
    listKind: UserList
    plural: users
    singular: user
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: User is the Schema for the users API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: UserSpec defines the desired state of User
            properties:
              accessKey:
                description: AccessKey for this user. It's read-only after creation.
                  This field is used to specify the user's access Key. This key is
                  used to access OSS. If you do not specify an accesskey, the accessKey
                  will be automatically generated by Controller.
                type: string
              capacity:
                description: Capacity that user desired.
                properties:
                  bucketNumber:
                    default: 0
                    description: The user's number of buckets.
                    format: int64
                    minimum: 0
                    type: integer
                  objectCount:
                    default: 0
                    description: The user's number of objects.
                    format: int64
                    minimum: 0
                    type: integer
                  storage:
                    default: 0
                    description: The user's storage space. The unit is MB. The default
                      value is 0 which means unlimited.
                    format: int64
                    minimum: 0
                    type: integer
                type: object
              provider:
                description: Provider name of a oss store. It's read-only after creation.
                  The controller will create the corresponding storage resources based
                  on this provider.
                type: string
              region:
                description: Region of oss store. It's read-only after creation. The
                  controller will create the corresponding storage resources based
                  on this region.
                type: string
              secretKey:
                description: SecretKey for this user. It's read-only after creation.
                  This field is used to specify the user's secret Key. This key is
                  used to access OSS. If you do not specify an secretkey, the secretKey
                  will be automatically generated by Controller.
                type: string
            required:
            - provider
            - region
            type: object
          status:
            description: UserStatus defines the observed state of User
            properties:
              usedCapacity:
                description: The user's capacity observed by the controller.
                properties:
                  bucketNumber:
                    default: 0
                    description: The user's number of buckets.
                    format: int64
                    minimum: 0
                    type: integer
                  objectCount:
                    default: 0
                    description: The user's number of objects.
                    format: int64
                    minimum: 0
                    type: integer
                  storage:
                    default: 0
                    description: The user's storage space. The unit is MB. The default
                      value is 0 which means unlimited.
                    format: int64
                    minimum: 0
                    type: integer
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
